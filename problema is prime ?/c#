     public static bool IsPrime(int n)
        {

            int numardiv = 0;
            for (int i = 1; i <= n / 2; i++)
            {

                if (n % i == 0) numardiv += 1;

            }
            if (numardiv == 1) return true;
            else return false;
        }
